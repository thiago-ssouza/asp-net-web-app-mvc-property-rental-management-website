@model IEnumerable<ASPNetWebAppMvcPRMWebsite.Models.ScheduleAppointment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ScheduleAppointmentId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PropertyManagerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TenantId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ScheduleAppointmentDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduleAppointmentId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PropertyManagerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TenantId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ScheduleAppointmentDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SA_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.SA_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SA_Id })
        </td>
    </tr>
}

</table>
